table:
  name: engine_types
  schema: public
array_relationships:
  - name: engines
    using:
      foreign_key_constraint_on:
        column: engine_type
        table:
          name: engines
          schema: public
